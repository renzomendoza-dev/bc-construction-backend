spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  error:
    include-stacktrace: always
    include-message: always

logging:
  level:
    com.bcconstructionservices: DEBUG
