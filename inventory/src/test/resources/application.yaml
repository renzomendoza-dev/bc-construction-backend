spring:
  # datasource: supplied by test-support's embedded Postgres
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true
    locations: classpath:db/migration

logging:
  level:
    org.hibernate.orm.jdbc.bind: TRACE